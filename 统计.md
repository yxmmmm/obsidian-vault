2025
```dataviewjs
const calendarData = {
    year: 2025,
    boxSize: 10,       // 强制正方格
    showCurrentDayBorder: true,
    colors: {
        blue: ["#c6e48b", "#7bc96f", "#239a3b", "#196127", "#103319"],
    },
    entries: []
}

// 重点：dv.pages() 括号里留空，代表统计全库所有文件
for (let page of dv.pages()) { 
    // 获取文件的创建日期 (ctime) 并格式化为 YYYY-MM-DD
    // 这样不管你的笔记叫什么名字，只要你在那天写了，就会被计入
    const dateStr = page.file.ctime.toFormat("yyyy-MM-dd");
    
    calendarData.entries.push({
        date: dateStr,
        intensity: 1, // 只要有笔记就亮起
        content: "" 
    })
}

renderHeatmapCalendar(this.container, calendarData)
```
