#算法#二分

# 整数二分

## 思想与方法

![算法-4](https://tuchuang-1387570672.cos.ap-nanjing.myqcloud.com/obsidianvault%E7%AE%97%E6%B3%95-4.jpg)

## 代码实现

```cpp
bool check(int x)//检查x是否满足该性质
  
//red
int bsearch1(int l,int r)
  {
    int mid=l+r+1>>1;
    if(check(mid))l=mid;
    else r=mid-1;
    return l;
  }
//yellow
int bsearch2(int l,int r)
{
  int mid=l+r>>1;
  if(check(mid))r=mid;
  else l=mid+1;
  return l;
}
```

# 浮点数二分

由于可平均分配，不存在边界问题

## 代码实现

```cpp
bool check(double x) {/* ... */} // 检查x是否满足某种性质

double bsearch_3(double l, double r)
{
    const double eps = 1e-6;   // eps 表示精度，取决于题目对精度的要求
    while (r - l > eps)
    {
        double mid = (l + r) / 2;
        if (check(mid)) r = mid;
        else l = mid;
    }
    return l;
}
```

