| 12  | 字符串_unicode字符集_三种创建字符串的方式_len()    | 9分54秒  | ✓   |
| --- | ---------------------------------- | ------ | --- |
| 13  | 字符串_转义字符_字符串拼接_字符串复制_input()获取键盘输入 | 10分29秒 | ✓   |
| 14  | 字符串_str()_字符提取_replace()替换_内存分析    | 6分42秒  | ✓   |
| 15  | 字符串切片slice操作_逆序                    | 7分20秒  | ✓   |
| 16  | 字符串_split()分割_join()合并_代码效率测试      | 9分33秒  | ✓   |
|     |                                    |        |     |
| 1   | 字符串_常用查找方法_去除首尾信息_大小写转换_排版_特征判断    | 10分10秒 | ✓   |
| 2   | 字符串_format格式化_数字格式化操作              | 8分34秒  |     |
| 3   | 可变字符串_io.StringIO                  | 4分16秒  |     |
| 4   | 类型转换总结                             | 2分49秒  |     |
| 5   | 列表_特点_内存分析                         | 7分11秒  |     |
| 6   | 创建列表的4种方式_推导式创建列表                  | 12分40秒 |     |
| 7   | 列表_元素的5种添加方式_效率问题                  | 9分7秒   |     |
| 8   | 列表删除的三种方式_删除的本质是元素拷贝               | 5分46秒  |     |
| 9   | 列表_元素的访问_出现次数统计_成员资格判断             | 8分30秒  |     |
| 10  | 切片slice操作                          | 7分33秒  |     |
| 11  | 列表_遍历_排序_max_min_sum               | 10分48秒 |     |
| 12  | 列表_二维列表_表格数据存储和读取                  | 7分5秒   |     |
| 13  | 元组_特点_创建的两种方式_tuple()要点            | 5分24秒  |     |
| 14  | 元组_元素访问_计数方法_切片操作_成员资格判断_zip()     | 5分45秒  |     |
| 15  | 元组_生成器推导式创建元组_总结                   | 8分13秒  |     |
| 16  | 字典_特点_4种创建方式_普通_dict_zip_fromkeys  | 13分12秒 |     |
| 17  | 字典_元素的访问_键的访问_值的访问_键值对的访问          | 5分30秒  |     |
| 18  | 字典_元素的添加_修改_删除                     | 7分20秒  |     |
| 19  | 字典_序列解包用于列表元组字典                    | 4分27秒  |     |
### 字符串
字符串的本质：字符序列
Python里没有单字符这个概念，单字符也是作为字符串来使用
Python的字符串是不能进行修改的

#### Unicode
Unicode是一个字符库，Python可以直接使用，包含世界上所有的语言，ASCII码，台湾繁体等

内置函数ord()可将汉字转换成Unicode码
内置函数chr()可将十进制数字转换成相应的字符

#### 用引号创建字符串
用单引号或双引号来创建字符串
```
a="abc"
b='我爱Python'
```
当字符串中也包含引号，需要让串内的引号和创建的引号一个为单引号一个为双引号或者使用转移字符"\"反斜杠
```
c='I'm "Tom"'
>>>I'm "Tom"
或
c="I'm \"Tom\""
```
使用连续三个引号可以创建多行字符串，并且保留字符串内容的格式
```
d="""
     I
           LOVE
	            PYTHON
"""
>>>
     I
		   LOVE
			    PYTHON
```

#### 空字符串和len()函数
用len()来测算字符串的长度
Python中允许空字符串的存在
空字符串的长度为0
```
a=""
print(len(a))  #结果是0

b="abc我爱你"
print(len(b))  #结果是6
```


#### 转义字符
```
\
```
我们可以使用`\+特殊字符`，实现某些难以用字符表示的效果。比如：换行等。常见的转义字符有这些：

| **转义字符** | **描述**        |
| -------- | ------------- |
| (在行尾时)   | 续行符           |
| \        | 反斜杠符号         |
| \'       | 单引号           |
| \"       | 双引号           |
| \b       | 退格(Backspace) |
| \n       | 换行            |
| \t       | 横向制表符         |
| \r       | 回车            |

#### 字符串的拼接
+可以将字符串进行拼接，将数字进行运算
如果+两边的类型不相同就会报错
多个字符串直接放在一起也会进行拼接
```
a="aa"
b="bb"
c=1
d=2
print(a+b)   #结果是aabb
print(c+d)   #结果是3
print(a+c)   #+两边类型不相同报错
'''
"aa""bb"    #结果为aabb
```

#### 字符串复制
字符串可以通过* 进行复制
a = 'Sxt'*3  #结果:'SxtSxtSxt'

#### **不换行打印**

我们前面调用print时，会自动打印一个换行符。有时，我们不想换行，不想自动添加换行符。我们可以自己通过参数end = “任意字符串”。实现末尾添加任何内容：

```
print("sxt",end=' ')
print("sxt",end='##')
print("sxt")

#结果 sxt sxt##sx
```

#### **从控制台读取字符串**

我们可以使用input()从控制台读取键盘输入的内容。

#### replace()实现对字符串的替换
```
>>> a = 'abcdefghijklmnopqrstuvwxyz'
>>> a
'abcdefghijklmnopqrstuvwxyz'
>>> a=a.replace("c","高")
'ab高defghijklmnopqrstuvwxyz'

```
字符串不能改变只能创建一个新的字符串，

#### str()可将其他类型转变成字符串

#### **使用[]提取字符**

字符串的本质就是字符序列，我们可以通过在字符串后面添加[]，在[]里面指定偏移量，可以提取该位置的单个字符。

1. 正向搜索：
    
    最左侧第一个字符，偏移量是0，第二个偏移量是1，以此类推。直到len(str)-1为止。
    
2. 反向搜索：
    
    最右侧第一个字符，偏移量是-1，倒数第二个偏移量是-2，以此类推，直到-len(str)为止。

```
>>> a = 'abcdefghijklmnopqrstuvwxyz'
>>> a
'abcdefghijklmnopqrstuvwxyz'
>>> a[0]    #正着数a[0]是a，a[25]是z，正着数从0开始
'a'
>>> a[3]
'd'
>>> a[26-1]
'z'
>>> a[-1]   #倒着数a[-1]是s，a[-26]是a，正着数从-1开始
'z'
>>> a[-26]
'a'
>>> a[-30]
Traceback (most recent call last):
  File "", line 1, in 
   a[-30]
IndexError: string index out of range   #如果索引不在范围内会报错

```

#### **字符串切片slice操作**

![image-20211026165701518](https://www.itbaizhan.com/wiki/imgs/image-20211026165701518.png)

切片slice操作可以让我们快速的提取子字符串。标准格式为：

`[起始偏移量start：终止偏移量end：步长step]`

典型操作(三个量为正数的情况)如下：

|操作和说明|示例|结果|
|---|---|---|
|`[:]` 提取整个字符串|“abcdef”[:]|“abcdef”|
|`[start:]`从start索引开始到结尾|“abcdef”[2:]|“cdef”|
|`[:end]`从头开始知道end-1|“abcdef”[:2]|“ab”|
|`[start:end]`从start到end-1|“abcdef”[2:4]|“cd”|
|`[start:end:step]`从start提取到end-1，步长是step|“abcdef”[1:5:2]|“bd”|

其他操作（三个量为负数）的情况：

|示例|说明|结果|
|---|---|---|
|"abcdefghijklmnopqrstuvwxyz"[-3:]|倒数三个|“xyz”|
|"abcdefghijklmnopqrstuvwxyz"[-8:-3]|倒数第八个到倒数第三个(包头不包尾)|'stuvw'|
|"abcdefghijklmnopqrstuvwxyz"[::-1]|步长为负，从右到左反向提取|'zyxwvutsrqponmlkjihgfedcba'|

切片操作时，起始偏移量和终止偏移量不在[0,字符串长度-1]这个范围，也不会报错。起始偏移量小于0则会当做0，终止偏移量大于“长度-1”会被当成-1。
例如：

```
>>> "abcdefg"[3:50]   #等价于"abcdefg"[3:7]包含最后一个字符
'defg'   

```

我们发现正常输出了结果，没有报错。

切记：包头不包尾，步长是每几个取，不是中间隔几个，起始值要加1个算起，因为第一个是a[0]

#### **split()分割和join()合并**

split()可以基于指定分隔符将字符串分隔成多个子字符串(存储到列表中)。如果不指定分隔符，则默认使用空白字符(换行符/空格/制表符)。示例代码如下：


```
>>> a = "to be or not to be"
>>> a.split()
['to', 'be', 'or', 'not', 'to', 'be']
>>> a.split('be')
['to ', ' or not to ', '']
```

有指定分隔符时，分隔符不在显示，👆be在作为分隔符时不在打印

join()的作用和split()作用刚好相反，用于将一系列子字符串连接起来。示例代码如下：
```
>>> a = ['sxt','sxt100','sxt200']
>>> '*'.join(a)
'sxt*sxt100*sxt200'

```

拼接字符串要点： 使用字符串拼接符`+`，会生成新的字符串对象，因此不推荐使用`+`来拼接字符串。推荐使用`join`函数，因为`join`函数在拼接字符串之前会计算所有字符串的长度，然后逐一拷贝，仅新建一次对象。

```
import time
​
time01 = time.time()  #起始时刻
a = ""
for i in range(1000000):
   a += "sxt"
​
time02 = time.time()  #终止时刻
​
print("运算时间："+str(time02-time01))
​
time03 = time.time()  #起始时刻
li = []
for i in range(1000000):
   li.append("sxt")
​
a = "".join(li)
​
time04 = time.time()  #终止时刻
​
print("运算时间："+str(time04-time03))

```

#### 字符串驻留机制
常量字符串只保留一份，它们的地址都一样
用is来判断对象是否相同，用== 来判断对象的值是否相同
```
c = "dd#"
d = "dd#"
print(c is d)   #True

```

用in来判断某个字符或者子字符串是否在字符串中
```
"ab" in "abcdefg"   #true

```

#### 字符串常用查找方法

我们以一段文本作为测试：

a='''我是高淇,我在北京尚学堂科技上班。我的儿子叫高洛希，他6岁了。我是一个编程教育的普及者，希望影响6000万学习编程的中国人。我儿子现在也开始学习编程，希望他18岁的时候可以超过我'''

| 方法和使用示例              | 说明             | 结果    |
| -------------------- | -------------- | ----- |
| len(a)               | 字符串长度          | 96    |
| a.startswith('我是高淇') | 以指定字符串开头       | True  |
| a.endswith('过我')     | 以指定字符串结尾       | True  |
| a.find('高')          | 第一次出现指定字符串的位置  | 2     |
| a.rfind('高')         | 最后一次出现指定字符串的位置 | 29    |
| a.count("编程")        | 指定字符串出现了几次     | 3     |
| a.isalnum()          | 所有字符全是字母或数字    | False |
a.find()返回的是字符串第一次出现的起始下标，而不是“单个字符”的下标
`s = 'abc6000def'`
`idx = s.find('6000')   # 查找的是连续 4 个字符组成的子串`
`print(idx)             # 输出 3`

子串 '6000' 从原串下标 3 开始连续出现，所以返回 3。
如果换成 s.find('60')，同样返回 3；换成 s.find('0') 则返回 5。

6000” 并不是“一个字符”，而是一个长度为 4 的子串；find 只关心它第一次出现的起始位置，不关心它本身有多长。

#### 字符串的格式化
format()
基本语法是通过 `{}` 和`:`来代替以前的 `%` 。
`format()` 函数可以接受不限个数的参数，位置可以不按顺序。
```
a="名字是:{0},年龄是:{1}"
b=a.format("秦朗",18)   #名字是:秦朗，年龄是：18
c=a.format("秦雨",9)   # 名字是：秦雨，年龄是：9
d="名字是:{0},年龄是:{1},{0}是个帅小伙"
e=a.format("秦朗"，18)   #名字是：秦朗，年龄是：18，是个帅小伙
```
索引，要按照顺序，相当于设置一个参数然后传参
也可以不按照顺序，但要把名字写出来
```
a="名字是:{name},年龄是:{age}"
b=a.format(age=18,name="秦朗")  #名字是:秦朗,年龄是:18     
```

#### 填充和对齐
^ 、 < 、  >分别代表着居中、左对齐和右对齐
 `:`号后面带填充的字符，只能是一个字符，不指定的话默认是用空格填充
```
 >>>"{:*>8}".format("234")  #冒号后面的填充,用*来填充，填充满8格，>让格式化的234右对齐
 '*****234'
 #输出'我是秦朗,我喜欢数字**666***'
 "我是{0}，我喜欢数字{:*^8}".format("秦朗",666)
```

#### 数字格式化
| 数字         | 格式      | 输出        | 描述                |
| ---------- | ------- | --------- | ----------------- |
| 3.1415926  | {:.2f}  | 3.14      | 保留小数点后两位          |
| 3.1415926  | {:+.2f} | 3.14      | 带符号保留小数点后两位       |
| 2.71828    | {:.0f}  | 3         | 不带小数              |
| 5          | {:0>2d} | 05        | 数字补零 (填充左边, 宽度为2) |
| 5          | {:x<4d} | 5xxx      | 数字补x (填充右边, 宽度为4) |
| 10         | {:x<4d} | 10xx      | 数字补x (填充右边, 宽度为4) |
| 1000000    | {:,}    | 1,000,000 | 以逗号分隔的数字格式        |
| 0.25       | {:.2%}  | 25.00%    | 百分比格式             |
| 1000000000 | {:.2e}  | 1.00E+09  | 指数记法              |
| 13         | {:10d}  | 13        | 右对齐 (默认, 宽度为10)   |
| 13         | {:<10d} | 13        | 左对齐 (宽度为10)       |
| 13         | {:^10d} | 13        | 中间对齐 (宽度为10)      |
不要过于深究也不要背，碰到的时候过来查
记住.2f是保留小数点后两位

#### 可变字符串   io.StringIO
Python中的字符串不能原地修改，如果修改必须要创建新的字符串对象
确实需要原地修改字符串，可以使用io.StringIO对象或array对象

```
import io
s = "abcdefghijklmn"
sio = io.StringIO(s)  #sio就是可变字符串
print(sio)  #得到一个内存地址
#得到对象的内容
print(sio.getvalue())
sio.seek(3)   #指针到索引3这个位置
sio.write("***")
print(sio.getvalue())   #结果 abc***ghijklmn

#.getvalue()来获取储存在字符串中的内容
```
总结：
1. `seek(n)` → 从索引n开始（包含这个位置）
    
2. 写入几个字符 → 覆盖几个字符
    
3. 如果写入长度超过剩余长度，会扩展字符串



