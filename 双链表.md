#算法 #链表

![算法-19](https://tuchuang-1387570672.cos.ap-nanjing.myqcloud.com/obsidianvault%E7%AE%97%E6%B3%95-19.jpg)

```cpp
#include <iostream>
using namespace std;
const int N = 1e6 + 10;

int m;
int l[N], r[N], e[N], idx;

// 初始化
void init()
{
    // 0表示左端点，1表示右端点
    r[0] = 1;
    l[1] = 0;
    idx = 2; // 从2开始
}

// 在k点右侧添加
void add(int k, int x)
{
    e[idx] = x;
    r[idx] = r[k];
    l[idx] = k;
    l[r[k]] = idx; // 先改l[r[k]]因为后面r[k]改变；注意k的下一个不一定是k+1
    r[k] = idx;
}

// 删除第k个点
void remove(int k, int x)
{
    r[l[k]] = r[k];
    l[r[k]] = l[k];
}
```

