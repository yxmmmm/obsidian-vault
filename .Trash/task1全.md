# Problem C: Task 1 - 基于贝叶斯推断的隐形粉丝投票重构

## 1. 引言与方法 (Introduction & Approach)
Task 1 的首要目标是基于观测到的裁判评分 $J_{i,t}$ 和二元淘汰结果 $\mathcal{E}_t$，重构未知的粉丝投票分布 $V_{i,t}$。

不同于传统的回归方法将票数视为确定性数值，我们采用 **生成式贝叶斯框架 (Generative Bayesian Framework)**。我们将无法观测的粉丝投票视为 **隐变量 (Latent Variables)**，且这些变量必须满足由淘汰规则施加的 **“硬约束” (Hard Constraints)**。这种方法不仅使我们能够估计最可能的投票占比，还能严格量化这些估计值的 **不确定性 (Uncertainty)**。

## 2. 模型构建 (Model Formulation)

### 2.1 变量与假设
记 $V_{i,t}$ 为选手 $i$ 在第 $t$ 周的粉丝投票占比。由于原始票数的绝对量级无法观测，且在排名规则下具有尺度不变性，我们对其进行归一化处理，使得 $\sum_i V_{i,t} = 1$。

我们要做出以下假设：
* **假设 1 (对数正态先验 Log-Normal Prior):** 粉丝人气通常服从长尾分布。我们将粉丝投票的先验建模为 $V_{i,t} \sim \text{LogNormal}(\mu, \sigma^2)$，这反映了“超级明星”效应，即少数选手拥有绝大多数选票。
* **假设 2 (归一化 Normalization):** 为解决尺度不可识别性问题，我们将研究对象锁定为 **投票占比 (Vote Share)** 而非原始票数。
* **假设 3 (硬淘汰约束 Hard Elimination Constraint):** 观测到的淘汰结果是“基本真理 (Ground Truth)”。任何有效的粉丝投票集合 $\{V_{i,t}\}$ 必须在数学上精确导致现实中观测到的淘汰结果。

### 2.2 贝叶斯推断结构
我们需要求解后验分布 $P(V | \text{Data})$。根据贝叶斯定理：
$$P(V_{i,t} | J_{i,t}, \mathcal{E}_t) \propto P(\mathcal{E}_t | V_{i,t}, J_{i,t}) \cdot P(V_{i,t})$$

其中：
* **先验 $P(V_{i,t})$:** 代表我们在看到结果之前对投票分布的信念（对数正态）。
* **似然 $P(\mathcal{E}_t | \dots)$:** 这是一个示性函数（硬约束）。
    $$P(\mathcal{E}_t | V, J) = \begin{cases} 1 & \text{若规则 } \text{Rule}(V, J) \text{ 导致观测到的淘汰结果} \\ 0 & \text{否则} \end{cases}$$

## 3. 算法：约束满足 MCMC (Constraint-Satisfied MCMC)
由于似然函数是不连续的（有效/无效），标准的梯度推断方法失效。我们采用带有拒绝核的 **Metropolis-Hastings MCMC** 算法在解空间中进行采样。

### Algorithm 1: 粉丝投票的贝叶斯重构
1.  **初始化 (Initialization)**: 为所有选手生成随机投票占比 $V^{(0)}$。
2.  **迭代 (Iteration)**: 对于 $k = 1$ 到 $N$:
    * **提议 (Proposal)**: 通过向 $V^{(k-1)}$ 添加微小随机噪声提出新状态 $V'$。
    * **约束检查 (Check Constraints)**:
        * 利用 $J_{t}$ 和 $V'$ 计算总分。
        * 验证总分最低的选手是否与数据集 $\mathcal{E}_t$ 中的实际淘汰者一致。
    * **接受/拒绝 (Accept/Reject)**:
        * 若有效: 接受 $V^{(k)} = V'$。
        * 若无效: 拒绝，保持 $V^{(k)} = V^{(k-1)}$ (或重新提议)。
3.  **聚合 (Aggregation)**: 计算预热期 (Burn-in) 后接受样本 $\{V^{(k)}\}$ 的均值和方差。

## 4. 结果：重构分布 (Results)

### 4.1 粉丝投票分布 (Violin Plot)
下图展示了第 X 周粉丝投票占比的概率密度。
*(此处插入你的小提琴图)*
* **观察:** 处于排行榜顶端的选手表现出更宽的分布（扁平形状），表明只要他们处于“安全区”，其具体的票数具有很大的弹性。
* **观察:** 处于淘汰边缘的选手显示出尖锐的峰值，因为他们的票数被生存阈值严格限制。

### 4.2 时间动态 (Ribbon Plot)
*(此处插入你生成的 Ribbon Plot/置信带图)*
图 2 展示了带有 95% 置信区间（阴影区域）的投票占比随时间演变的情况。这一动态过程揭示了粉丝支持率是如何随着比赛进程而转移的。

## 5. 不确定性量化 (Uncertainty Quantification)

**问题: 粉丝投票总数的确定性有多大？这种确定性是否总是相同的？**

**回答: 不，确定性是高度不均匀的 (Highly Non-uniform)。**
我们的分析表明，确定性是一个动态属性，取决于选手距离“淘汰阈值 (Elimination Threshold)”的远近。

我们使用从 MCMC 后验样本中导出的两个指标来量化确定性：
1.  **95% 置信区间宽度 (CIW)**: 覆盖 95% 可能投票占比的范围。
2.  **变异系数 (CV)**: 定义为 $CV = \sigma / \mu$。

### 主要发现:
1.  **“边缘效应” (The Edge Effect - 高确定性):**
    尽管裁判评分很高但仍被淘汰（或勉强生存）的选手（例如 Season 1 Week 4 的 Rachel Hunter），表现出 **极高的确定性** (低 CV)。
    * *推理解释:* 对于一个高分名流来说，要被淘汰，其粉丝投票 *必须* 极低。数学约束将可行解空间压缩在一个极窄的范围内，几乎没有不确定性的余地。

2.  **“安全效应” (The Safety Effect - 低确定性):**
    统治级选手（高裁判分 + 高粉丝票）表现出 **低确定性** (高 CV / 宽置信区间)。
    * *推理解释:* 如果一名选手稳居第一，无论他获得 30% 还是 40% 的选票，在数学上都同样满足约束条件。因此，模型正确地报告了较高的不确定性，这反映了观测数据中信息的缺失。

**结论:** 我们估计中的不确定性并非模型的缺陷，而是排名系统的固有特征。只有当选手处于淘汰边缘时，淘汰机制才会揭示有效信息。