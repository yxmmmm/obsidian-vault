### 1. 边坡位移监测数据异常清洗与校正模型（问题 1）

#### 1.1 问题分析与预处理方案设计

问题 1 要求对新型光纤位移计测得的数据 A 进行校正，使其尽可能逼近作为基准参考的传统振弦式位移计数据 B。在对附件 1 的原始位移时序数据进行初步可视化探索后，发现数据存在以下显著特征：

1. **比例偏差与零漂：** 数据 A 整体呈发散状偏离数据 B，说明光纤传感器除了存在固定的截距零漂外，还存在灵敏度（斜率）偏差。
2. **高频噪声与同频突变（关键陷阱）：** 虽然 B 为基准参考数据，但 A、B 在若干时刻出现同步下跳点。从边坡位移演化的连续性和单调性角度看，这些点不符合真实物理变形规律，因此将其视为共同异常扰动点，不直接参与校正映射拟合，而采用邻域趋势重构其物理位移估计值。值得注意的是，通过时序截面对比发现，**表 1.1 中要求验证的 5 个数据 A，恰好完全对应于发生严重下跳故障的时间节点。**

若直接利用包含异常值的原始数据进行回归拟合，或者将表 1.1 中的故障读数直接代入校正方程，将导致边坡位移估计产生上百毫米的致命误差。因此，必须构建“时序异常清洗—保形插值补全—空间映射回归”三步走耦合模型，以获取符合物理演化规律的校正结果。

#### 1.2 监测数据异常值清洗与插值模型

鉴于真实的边坡表面位移演化在宏观上具有连续性和平滑单调性，本文引入**滑动中位数检测算法**与分段三次埃尔米特保形插值（PCHIP）算法对数据 A 和 B 进行同步清洗。

**（1）基于滑动窗口的异常值检测**

设原始位移时序序列为 $D = \{d_1, d_2, \cdots, d_N\}$，构建窗口大小为 $W$ 的滑动观测区间。对于时刻 $t_i$，其局部中位数 $M_i$ 计算如下：

$$M_i = \text{Median}(d_{i - \lfloor W/2 \rfloor}, \cdots, d_i, \cdots, d_{i + \lfloor W/2 \rfloor})$$

考虑到中位数对极端离群值具有极强的鲁棒性，计算残差序列 $r_i = d_i - M_i$。设定偏差阈值 $\varepsilon$（本文取 $\varepsilon = -3.0$ mm），当 $r_i < \varepsilon$ 时，判定该点为向下突变的设备故障点，并将其标记为空值（NaN）。本研究将窗口 $W=15$，对应约 2.5 小时的局部观测区间，中心点前后各约 70 分钟。

**（2）分段三次埃尔米特保形插值 (PCHIP)**

对于剔除异常点后产生的缺失区段，本文未采用常规的三次样条插值，而是采用 PCHIP 算法。由于样条插值在陡变数据段容易产生“超调”震荡，而 PCHIP 能够严格保持插值曲线在缺失区段与前后数据的**单调性一致**，这完全契合边坡位移不可逆的物理实质。清洗并插值后，得到还原真实物理演化趋势的干净序列集 $A_{clean}$ 与 $B_{clean}$。

#### 1.3 校正模型的构建与求解

基于清洗后的有效数据集 $A_{clean}$ 与 $B_{clean}$，建立一元线性回归映射模型，以修正光纤位移计的灵敏度系数与初始零漂。假设校正后的数据 $y$ 与清洗后的数据 A 满足如下线性关系：

$$y = k \cdot A_{clean} + b$$

式中，$k$ 为灵敏度校正系数（拟合直线斜率），$b$ 为零漂补正截距。采用普通最小二乘法（OLS）极小化残差平方和，求解目标函数：

$$\min_{k, b} \sum_{i=1}^{M} (B_{clean}^{(i)} - (k \cdot A_{clean}^{(i)} + b))^2$$

利用软件进行数值求解，得出模型参数为：

- **灵敏度校正系数**：$k = 0.8815$
- **零漂补正截距**：$b = -1.2771$

即最终的数学校正模型为：

$$y = 0.8815 \cdot x_{clean} - 1.2771$$

其中 $x_{clean}$ 是经滑动中位数异常检测和 PCHIP 插值恢复后的光纤位移计趋势值，而非异常原始读数

#### 1.4 模型交叉验证与偏差量化分析

为避免随机划分破坏时间序列结构，本文采用时间分块 5 折交叉验证。将清洗后的时序数据按时间顺序等分为 5 个连续子区间，每次选取其中 4 个区间作为训练集，剩余 1 个区间作为测试集，循环 5 次。最终对 5 次测试集预测结果进行汇总，计算整体 MAE、RMSE 与 $R^2$。

| 指标  | 交叉验证结果 |
| ----- | ------------ |
| MAE   | 约 1.32 mm   |
| RMSE  | 约 2.01 mm   |
| (R^2) | 约 0.9996    |

极高的 $R^2$ 值（接近 1）表明，经时序清洗剔除异常跳变后，新型光纤位移计数据与基准数据之间存在极度显著的线性映射关系，验证了该线性校正模型的科学性与准确性。

**（此处插入你用 MATLAB 画的 图1：散点拟合图）**

*图 1：清洗后光纤位移计数据 A 与基准数据 B 的线性映射关系*

**（此处插入你用 MATLAB 画的 图2：时序对比验证图）**

*图 2：数据校正模型时序效果对比及表 1.1 验证点分布*

#### 1.5 表 1.1 结果计算与说明

针对表 1.1 中提供的 5 个校正前数据 $x$，如前文所述，经核查这些点均发生于传感器同时出现“断崖式”读数异常的时刻。依据本模型的逻辑，不能将错误的故障原始值直接代入空间回归方程。正确的计算准则为：**提取该时刻经清洗及保形插值后的真实物理位移估计值 $A_{clean}$，将其作为模型的有效输入 $x_{clean}$，代入校正方程进行计算。**

计算过程举例（以 $x = 18.526$ 为例）：

该异常值产生于 2024 年 3 月 2 日 11:00。经 PCHIP 算法填补，该时刻光纤数据 A 的真实趋势值应为 $343.931$ mm。代入校正模型：

$$y = 0.8815 \times 343.931 - 1.2771 \approx 301.895 \text{ mm}$$

以此类推，得出表 1.1 的最终数据校正结果如下表所示：

**表 1.1 问题 1 数据校正结果**

| **校正前数据 x**   | **7.132**  | **18.526**  | **84.337** | **123.554** | **167.667** |
| ------------------ | ---------- | ----------- | ---------- | ----------- | ----------- |
| **校正后数据 $y$** | **45.489** | **301.895** | **76.389** | **161.839** | **274.581** |