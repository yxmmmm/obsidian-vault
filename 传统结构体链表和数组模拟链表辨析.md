#数据结构 #链表 

| **数组模拟** | **传统结构体版** | **含义**             |
| ------------ | ---------------- | -------------------- |
| `h[u]`       | `head`           | 指向第一条边的指针   |
| `ne[i]`      | `ptr->next`      | 指向下一条边的指针   |
| `e[i]`       | `ptr->val`       | 当前边指向的具体节点 |

虽然看上去一一对应，但二者仍有区别，在图论写邻接表的时候才发现

- 数组模拟的h数组以及ne数组存储的是指针，也可以理解为指向元素的边；只有通过e数组才能得到对象的值；idx表示用到哪个节点
- 而传统结构体则更为直接，可直接通过->next操作访问下一元素的值

例如

```cpp
//访问头节点指向元素
head->next//传统结构体
e[h[u]]//数组模拟
  
//访问第二个元素
head->next->next//传统结构体
e[ne[h[u]]]//数组模拟
```



**头插法辨析应用**

```cpp
#include<bits/stdc++.h>
using namespace std;

//数组模拟(邻接表为例，刚好学到这hh)

```







