#数据结构 #链表 

| **数组模拟** | **传统结构体版** | **含义**             |
| ------------ | ---------------- | -------------------- |
| `h[u]`       | `head`           | 指向第一条边的指针   |
| `ne[i]`      | `ptr->next`      | 指向下一条边的指针   |
| `e[i]`       | `ptr->val`       | 当前边指向的具体节点 |

虽然看上去一一对应，但二者仍有区别，在图论写邻接表的时候才发现

