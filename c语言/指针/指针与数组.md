数组：一组相同类型元素的集合
指针变量：是一个变量，存放的是地址
```c
int main(){
	 int arr[10] = {0}
	 //arr为首元素地址相当于&arr[0]
	 int *p = arr;
	 
	 //通过指针访问数组
	 int sz = sizeof(arr)/sizeof(arr[o]);
	 int i = 0;
	 for(i = 0; i < sz; i++)
	 {
	 printf("%d", *(p+i))
	 }
 }//输出10个0
```


==printf("%d", arr[i]) = printf("%d", * (p+i)) = printf("%d", * (arr+i))==
