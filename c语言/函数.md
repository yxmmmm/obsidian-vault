# 形式参数
```c
int max(int x,int y)
```
# 实际参数
```c
int a=2,b=3,c;
c=max(a,b)
```
注:形参未调用不占存储单元，调用完后释放 
实惨有确定的值
被定义函数必须定义形参类型
![Pasted image 20251116124539|400](https://tuchuang-1387570672.cos.ap-nanjing.myqcloud.com/obsidianvaultPasted%20image%2020251116124539.png)
按顺序一一对应
- 形参和实参时不同存储单元
- 单项值传递![Pasted image 20251116125009](https://tuchuang-1387570672.cos.ap-nanjing.myqcloud.com/obsidianvaultPasted%20image%2020251116125009.png)
- 函数类型与返回值类型相同
  ```c
  int max(float x,float y){
  return x>=y?x:y}//函数类型和返回值类型不一致
  int main(){
  float a=4.5,b=3.8,c;
  c=max(a,b)
  printf("%.1f",c)//c的输出值为7.0
  }```
为表示不带返回值的函数可直接用void  
```c
void printstar(){
printf("******")};
```
# 函数调用
## 函数中调用其他函数
1. 被调函数存在
2. 调用库函数，使用#include
3. 被调函数自定义，则需进行[[声明]]

