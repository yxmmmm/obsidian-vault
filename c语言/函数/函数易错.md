# 1
![1551764258013_.pic_hd.jpg|200](https://tuchuang-1387570672.cos.ap-nanjing.myqcloud.com/obsidianvault1551764258013_.pic_hd.jpg)

逗号表达式：按顺序从左到右计算，输出最后一个表达式的计算结果x+y=13
func函数中z先用8的值计算，再自减（后缀自减）

---
# 2 c语言程序基本组成单位是函数
---

# 3判断数字每一位数字处理方法
本题要求实现一个统计整数中指定数字的个数的简单函数。

### 函数接口定义：

```c++
int CountDigit( int number, int digit );
```

其中`number`是不超过长整型的整数，`digit`为[0, 9]区间内的整数。函数`CountDigit`应返回`number`中`digit`出现的次数。

```c
int CountDigit( int number, int digit ){
    if(number < 0)
        number = -number;
    if(number == 0 && digit == 0)
        return 1;
    int count = 0;
    int temp = number;
    int t;
    for(int i = 0; temp > 0; i++){//temp > 0和temp%10区分
        t = temp%10;
        if(t == digit)
            count++;
        temp /= 10;
    }
    return count;
}
```
优化：***while语句***
```c
while (temp > 0) {
    int digit = temp % 10;
    sum += pow(digit, N);
    temp /= 10;
}
```



